import React from "react";
import { Card, CardHeader, Container } from "reactstrap";

const Booking = () => {
  return (
    <Container fluid="xl" className="text-center mt-5">
      <Card>
        <CardHeader>List of Booking</CardHeader>
      </Card>
    </Container>
  );
};

export default Booking;
